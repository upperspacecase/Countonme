rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Users: any authed user can read profiles, only owner can write
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    // Check-ins: any authed user can read (for the feed), only owner can write
    match /users/{userId}/checkins/{date} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    // Gratitude: any authed user can read (for card flip), only owner can write
    match /users/{userId}/gratitude/{entryId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
